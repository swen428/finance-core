-- Finance V3 SQLite application identity.
-- ASCII "FIN3" encoded as a stable signed-32-bit-safe integer.
PRAGMA application_id = 1179209267;

-- Validation guidance:
-- Apply only through the explicit migration safety boundary against a
-- temporary or explicitly approved target. Never test against finance.db.
